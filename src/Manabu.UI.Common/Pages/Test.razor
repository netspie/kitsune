@page "/test"

<div class="main-section w-full h-full flex flex-col p-5">
    <div class="w-full h-full flex flex-wrap gap-2 items-center justify-center" style="font-family: Arial">
        <div class="shadow-lg w-[50px] flex h-[50px] items-center justify-center text-2xl hover:scale-75">@GetRandomKanji()</div>
        <div class="shadow-lg w-[50px] flex h-[50px] items-center justify-center text-2xl hover:scale-75">@GetRandomKanji()</div>
        <div class="shadow-lg w-[50px] flex h-[50px] items-center justify-center text-2xl hover:scale-75">@GetRandomKanji()</div>
        <div class="shadow-lg w-[50px] flex h-[50px] items-center justify-center text-2xl hover:scale-75">@GetRandomKanji()</div>
        <div class="shadow-lg w-[50px] flex h-[50px] items-center justify-center text-2xl hover:scale-75">@GetRandomKanji()</div>
        <div class="shadow-lg w-[50px] flex h-[50px] items-center justify-center text-2xl hover:scale-75">@GetRandomKanji()</div>
        <div class="shadow-lg w-[50px] flex h-[50px] items-center justify-center text-2xl hover:scale-75">@GetRandomKanji()</div>
        <div class="shadow-lg w-[50px] flex h-[50px] items-center justify-center text-2xl hover:scale-75">@GetRandomKanji()</div>
        <div class="shadow-lg w-[50px] flex h-[50px] items-center justify-center text-2xl hover:scale-75">@GetRandomKanji()</div>
        <div class="shadow-lg w-[50px] flex h-[50px] items-center justify-center text-2xl hover:scale-75">@GetRandomKanji()</div>
        <div class="shadow-lg w-[50px] flex h-[50px] items-center justify-center text-2xl hover:scale-75">@GetRandomKanji()</div>
        <div class="shadow-lg w-[50px] flex h-[50px] items-center justify-center text-2xl hover:scale-75">@GetRandomKanji()</div>
        <div class="shadow-lg w-[50px] flex h-[50px] items-center justify-center text-2xl hover:scale-75">@GetRandomKanji()</div>
        <div class="shadow-lg w-[50px] flex h-[50px] items-center justify-center text-2xl hover:scale-75">@GetRandomKanji()</div>
        <div class="shadow-lg w-[50px] flex h-[50px] items-center justify-center text-2xl hover:scale-75">@GetRandomKanji()</div>
        <div class="shadow-lg w-[50px] flex h-[50px] items-center justify-center text-2xl hover:scale-75">@GetRandomKanji()</div>
    </div>
</div>

@code {

    static char GetRandomKanji()
    {
        var random = new Random();
        int randomCodePoint = random.Next(0x4E00, 0x4F00);
        char randomKanji = Convert.ToChar(randomCodePoint);
        return randomKanji;
    }

}
